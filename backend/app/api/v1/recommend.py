# 智能推荐 API 路由
# 功能：
# - GET /personalized     个性化推荐（基于用户浏览/收藏历史，协同过滤算法）
# - GET /similar/{restaurant_id}  相似餐厅推荐（基于内容相似度，考虑菜系/价格/评分/位置）
# - GET /scene            场景推荐（根据用餐场景：约会/聚餐/商务/家庭等，标签匹配）
# - GET /hot              热门推荐（综合评分、评论数、销量的热度排序）

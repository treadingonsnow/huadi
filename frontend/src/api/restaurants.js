// 餐厅相关 API 调用
// - searchRestaurants(params)          搜索餐厅 GET /restaurants/search
// - getRestaurantDetail(id)            获取详情 GET /restaurants/{id}
// - getRestaurantReviews(id, params)   获取评论 GET /restaurants/{id}/reviews
// - addFavorite(restaurantId)          收藏 POST /restaurants/favorites
// - removeFavorite(restaurantId)       取消收藏 DELETE /restaurants/favorites/{id}
// - getFavorites(params)               收藏列表 GET /restaurants/favorites
